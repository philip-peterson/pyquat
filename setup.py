#!/usr/bin/env python

from setuptools import setup

setup(name='Pyternion',
      version='1.0',
      description='Pythonic Quaternion library including Euclidean geometry calculations',
      author='Philip Peterson',
      packages=['pyternion'],
     )
